<div class="difficulty-section">
	<div class="difficulty-wrapper">
		<span class="header-emoji">🤨</span>
		<h1>What kind of a <br><span><img src="" alt="Image of the artist" class="artist-preview-image" /><span class="artist-name" data-name="artist"></span> fan are you?</span></h1>
		<p class="intro-p">Find out by listening to a short snippet and try to identify the song.</p>
		<div class="difficulty-container">
			<button data-length="5" class="selector difficulty-button ">
				<!-- <img src="/easy.png" alt="Emoji" class="difficulty-emoji">			 -->
				<span class="difficulty-emoji">🐥</span>
				<div class="difficulty-label">
					Kind of a fan
					<span>5 seconds of playback</span>
				</div>
			</button>
			<button data-length="3" class="selector difficulty-button ">
				<!-- <img src="/medium.png" alt="Emoji" class="difficulty-emoji">			 -->
				<span class="difficulty-emoji">😎</span>
				<div class="difficulty-label">
					Real fan
					<span>3 seconds of playback</span>
				</div>
			</button>
			<button data-length="1" class="selector difficulty-button ">
				<!-- <img src="/hard.png" alt="Emoji" class="difficulty-emoji">			 -->
				<span class="difficulty-emoji">🤘</span>
				<div class="difficulty-label">
					Hardcore fan
					<span>2 seconds of playback</span>
				</div>
			</button>
		</div>
	</div>
</div>

<style>

	.artist-preview-image{
		border: 2px solid var(--spotify);
	}	
	.artist-name{
		color: var(--spotify);
	}

	.difficulty-section {
		display: none;
		padding: var(--vertical-padding) var(--side-padding);
	}
	.difficulty-container{
		display: grid;
		gap: 0.75rem;
		justify-content: center;
	}
	
	.difficulty-label{
		display: flex;
		flex-direction: column;
		font-size: 1.25rem;
		gap:0.25rem;
	}
	button.difficulty-button>*{
		pointer-events: none;
	}
	.difficulty-label>span{
		display: block;
		font-weight: 400;
		font-size: 0.875rem;
		opacity: 0.7;
	}
	.difficulty-button{
		padding: 1.5rem;
		text-align: left;
		gap: 1.5rem;
	}
	.difficulty-emoji{
		display: flex;
		align-items: center;
		justify-content: center;
		font-size: 2.5rem;
		width: 2.5rem;
		height: 2.5rem;
	}
	img.artist-preview-image{
		width: clamp(2rem, 10vh, 6rem);
		height: clamp(2rem, 10vh, 6rem);
		border-radius: 99rem;
		margin-right: 1rem;
		overflow: hidden;
		display: none;
	}
	
	/* Desktop styles*/
	@media (min-width: 768px){
		.difficulty-wrapper {
			/* min-height: 100vh; */
			display: flex;
			place-content: center center;
			flex-direction: column;
			
		}
		.difficulty-container{
			grid-template-columns: repeat(3, 1fr);
		}
		
		.difficulty-button{
			padding: 1rem;
			text-align: center;
		}
		.difficulty-emoji{
			font-size: 3.75rem;
			width: 4rem;
			height: 4rem;
		}
		.difficulty-label{
			font-size: 1.75rem;
		}
		.difficulty-label>span{
			font-size: 1.125rem;
		}
		img.artist-preview-image{
			display: inline-block;
			width: clamp(2.5rem, 6vh, 5rem);
			height: clamp(2.5rem, 6vh, 5rem);
		}

		h1{
			--main-rotation: 1deg;
			--sub-rotation: -2deg;
		}
	}
</style>
