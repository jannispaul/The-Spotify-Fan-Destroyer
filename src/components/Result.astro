
<div class="result-section">
	<div class="result-wrapper">
		<h1 class="message">You were right</h1>
		<div class="song-card">
			<img src="" class="song-cover" />
			<p class="song-title" data-title="song-title"></p>
		</div>
		<button class="next-button">
			<span class="next-text">
				Next round
			</span>
			<svg width="100%" height="100%" class="icon large" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
				<path d="M4 11H16.17L10.58 5.41L12 4L20 12L12 20L10.59 18.59L16.17 13H4V11Z" fill="#181818"/>
			</svg>					
		</button> 
	</div>
</div>

<style>
.result-section {
	display: none;
	padding: calc(2 * var(--side-padding)) var(--side-padding);
}	
.result-wrapper{
	display: flex;
	flex-direction: column;
	width: 100%;
	align-items: center;
}
.song-card{
	padding: 1rem;
	border-radius: 1.25rem;
	border: 1px solid var(--black-selected);
	width: calc(100% - 2rem);
	max-width: calc(var(--mw-small) - 2rem);
	display: flex;
	flex-direction: column;
	gap: 1.25rem;
	background: var(--black);
	position: relative;
	margin-bottom: 2rem;
}
.song-cover{
	max-width: 100%;
	border-radius: 0.375rem;
	overflow: hidden;
	aspect-ratio: 1;
}
.song-card.correct{
	box-shadow: 0px 0px 250px 0px rgba(0, 189, 0, 0.15);
}

.song-card.incorrect{
	box-shadow: 0px 0px 250px 0px rgba(239, 0, 0, 0.15);
}
.next-text{
	pointer-events: none;
}

@media (min-width: 768px){
	.song-card{
		padding: 1.5rem;

	}
	.result-wrapper{
		gap: 5rem;
	}

}
</style>

<style is:global>

.song-card::after{
	position: absolute;
	font-size: 2rem;
	top: 0;
	right: 0;
	transform: rotateZ(10deg);
}

@media (min-width: 768px){

	.song-card::after{
		font-size: 3rem;
		top: -1rem;
		right: -1rem;
	}
}

</style>