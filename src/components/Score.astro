
<div class="score-container">
	<div class="score-wrapper">
		<div class="score-board">
			<div class="round-status"></div>
		</div>
	</div>
</div>

<style>
.score-container {
	display: none;
	padding-top: 2.5rem;
}	
.score-wrapper{
	display: flex;
	justify-content: center;
}
.score-board{
	display: flex;
	justify-content: center;
	align-items: center;
	gap: 1.25rem;
	background: var(--black);
	padding: 1rem 1.25rem;
	border: 1px solid var(--black-selected);
	border-radius: 0.4rem;
	transition: transform 300ms cubic-bezier(0.34, 1.56, 0.64, 1);
}

.score-board.scaled{
	transform: scale(1.3);
}
.round-status {
	width: 0.75rem;
	height: 0.75rem;
	background: var(--gray-dark);
	border-radius: 0.25rem;
	position: relative;

}
.round-status.active{
	background: var(--primary);
}
.round-status.correct::after{
	width: 1rem;
	content: "✅";
	position: absolute;
	top:-0.25rem;
	left:-0.25rem;
	z-index: 1;
	background: var(--black);
}

.round-status.incorrect::after{
	content: "❌";
	position: absolute;
	top:-0.25rem;
	left:-0.25rem;
	z-index: 1;
	background: var(--black);

}

</style>
